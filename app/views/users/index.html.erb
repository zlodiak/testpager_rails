<h1 >all users</h1>


<ul>
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user %>
      <%= link_to user.name, user %>
      <%= link_to 'delete', user, method: :delete, data: {confirm: 'sure?'} %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>